\chapter{系统安全设计}

\section{安全问题与设计思路}

前面几章主要讨论的是细粒度分类精度,但如果系统只在干净样本上表现良好,实际使用时仍然可能出现不稳定输出。视觉模型在推理阶段容易受到对抗性扰动影响,输入图像即使只发生很小的像素变化,最终预测也可能出现明显偏移。对本文这种基于 CLIP 的提示学习系统来说,这个问题不能简单地用“训练集上效果不错”来回避,因为推理阶段的输入并不总是理想条件下的干净图像。

本项目对安全部分的处理思路是,不在当前毕业设计阶段重新实现一套新的防御算法,而是直接复用 TTC 原始代码,再把它桥接到本文已经训练好的细粒度分类模型上。这样做有两个原因。第一,原始 TTC 的核心逻辑已经公开,直接复用更容易保证方法与论文保持一致。第二,本文的主要工作仍然是细粒度分类方法设计,安全部分更适合定位为一个推理阶段扩展模块,重点在于说明其接入方式和验证结果。

\section{TTC 方法原理}

本文采用的安全机制来自 Test-time Counterattack (TTC)\cite{xing2025ttc}。TTC 的基本思路不是在训练阶段大规模加入对抗样本,而是在测试阶段利用模型自身的视觉编码器,对输入图像施加一组反向扰动,观察并修正图像特征的异常变化。

在原始实现中,一个关键量是特征差异比率 \(\tau\),定义为
\begin{equation}
\tau = \frac{\lVert f_{att} - f_{orig} \rVert}{\lVert f_{orig} \rVert},
\end{equation}
其中 \(f_{orig}\) 表示原始输入的视觉特征,\(f_{att}\) 表示加入扰动后的视觉特征。如果某个输入在加入轻量扰动后特征变化很大,说明它在当前特征空间中并不稳定,更可能受到对抗性攻击影响。TTC 正是利用这一点,根据 \(\tau\) 的大小决定防御强度。

与普通的检测后丢弃不同,TTC 会继续对输入做反向修正。设输入图像为 \(X\),扰动为 \(\delta\),则系统在有界约束下多步更新 \(\delta\),使扰动后图像与原图在特征空间中的偏差受到控制。项目中采用的主要形式为 \(\ell_{\infty}\) 约束,对应的多步更新最终会得到一组候选扰动,再依据 \(\tau\) 对不同步的结果进行加权。这样得到的图像不是“恢复原图”,而是被推回到一个更稳定的特征区域。

\section{桥接实现方式}

\subsection{实现目标}

本文没有继续沿用项目早期那套项目内重写的防御接口,而是新建桥接脚本 \texttt{fine\_grained\_classification/evaluate\_security\_ttc.py},直接复用 \texttt{CLIP-Test-time-Counterattacks} 中的核心逻辑。脚本的目标很明确: 保留 TTC 的核心计算过程,只把数据来源和被测模型替换为当前项目的实现。

具体来说,桥接版本完成了三项工作。

\begin{enumerate}
\item 使用当前项目的 \texttt{OxfordPets} 测试集和 \texttt{DynamicPromptTrainer} checkpoint;
\item 将 Dassl/CoOp 输出的归一化图像反归一化回 \([0,1]\),再按 TTC 原始流程重新执行 \texttt{clip\_img\_preprocessing};
\item 在细粒度分类模型上生成 PGD 对抗样本,并分别评估 TTC 对干净样本和对抗样本的影响。
\end{enumerate}

\subsection{代码结构}

当前安全验证所涉及的关键文件如下:

\begin{itemize}
\item \texttt{fine\_grained\_classification/evaluate\_security\_ttc.py}: TTC 桥接评估脚本;
\item \texttt{CLIP-Test-time-Counterattacks/code/func.py}: 原始预处理与 CLIP 推理辅助函数;
\item \texttt{CLIP-Test-time-Counterattacks/code/attacks.py}: 原始攻击与 TTC 相关函数;
\item \texttt{fine\_grained\_classification/models/dynamic\_prompt.py}: 当前项目的提示学习模块;
\item \texttt{fine\_grained\_classification/output\_fgd/...}: 已训练好的提示学习 checkpoint。
\end{itemize}

为了让原始 TTC 代码适应当前环境,项目中只做了两类必要修改。其一,在桥接脚本中显式兼容当前 PyTorch 版本的 checkpoint 加载方式。其二,对原始 \texttt{attacks.py} 做了最小范围调整,去掉了对强制 \texttt{.cuda()} 的依赖,使其能够跟随输入张量所在设备运行。除此之外,TTC 的核心计算逻辑保持不变。

\subsection{推理与评估流程}

桥接脚本的完整评估流程如下:

\begin{enumerate}
\item 读取 \texttt{OxfordPets} 测试集;
\item 加载 \texttt{DynamicPromptTrainer} 的 \texttt{prompt\_learner} 参数;
\item 在当前模型上计算干净样本准确率;
\item 使用 PGD 生成对抗样本;
\item 分别对干净样本和对抗样本执行 TTC;
\item 记录 \texttt{clean\_acc}、\texttt{clean\_ttc\_acc}、\texttt{adv\_acc}、\texttt{adv\_ttc\_acc} 以及 \(\tau\) 统计。
\end{enumerate}

这套流程和本文主实验是分开的。分类实验关注的是不同提示学习方法在 few-shot 条件下的精度表现,安全实验关注的是当前模型在攻击前后、以及 TTC 介入前后的变化。两者各自回答不同的问题,但又能在同一个项目里衔接起来。

\section{实验设置与结果分析}

\subsection{实验设置}

当前安全实验使用的是 1-shot 设置下训练得到的 \texttt{DynamicPromptTrainer} 模型,测试集为 Oxford-IIIT Pets 全部 3669 张测试图像。攻击方式采用 PGD,攻击参数为 \(\epsilon = 1/255\)、步长 \(\alpha = 1/255\)、迭代步数 10。TTC 参数采用 \(\epsilon = 3/255\)、步长 \(1/255\)、迭代步数 2、阈值 \(\tau_{thres}=0.6\)、加权系数 \(\beta = 1.0\)。

该组参数是在多次试验后选出的。较早的配置虽然能带来一定防御增益,但存在两个问题: 一是干净样本精度下降明显,二是部分设置下对抗样本的 \(\tau\) 反而低于干净样本,说明判别信号并不稳定。将阈值调高到 0.6,同时把 TTC 扰动设置为 \(3/255\) 后,\(\tau\) 的行为更符合预期,结果也更有解释性。

\subsection{实验结果}

表 \ref{tab:ttc_result} 给出了当前安全实验的主要结果。

\begin{table}[H]
\centering
\caption{TTC 在 1-shot DynamicPromptTrainer 上的安全验证结果}
\label{tab:ttc_result}
\begin{tabular}{l c}
\toprule
指标 & 数值 \\
\midrule
干净样本准确率 (\texttt{clean\_acc}) & 85.85\% \\
干净样本经 TTC 后准确率 (\texttt{clean\_ttc\_acc}) & 68.06\% \\
对抗样本准确率 (\texttt{adv\_acc}) & 0.08\% \\
对抗样本经 TTC 后准确率 (\texttt{adv\_ttc\_acc}) & 71.63\% \\
对抗精度提升 (\texttt{adv\_ttc\_gain}) & 71.55 个百分点 \\
干净样本精度变化 (\texttt{clean\_ttc\_delta}) & -17.80 个百分点 \\
干净样本平均 \(\tau\) & 0.9453 \\
对抗样本平均 \(\tau\) & 1.0470 \\
\bottomrule
\end{tabular}
\end{table}

从结果上看,TTC 在当前模型上确实起到了明显的防御作用。PGD 攻击后,模型准确率几乎降到 0,说明细粒度提示学习模型在强攻击下并不具备天然鲁棒性。加入 TTC 后,对抗样本准确率恢复到 71.63\%,提升幅度达到 71.55 个百分点。单从防御效果看,这一结果已经足以说明 TTC 在本文模型上是有效的。

不过,这组结果也暴露出明显代价。干净样本准确率从 85.85\% 降到 68.06\%,下降了 17.80 个百分点。这意味着当前 TTC 配置虽然能显著恢复鲁棒性,但对正常输入的干扰仍然偏大。从系统角度说,这不是一个可以直接当作默认部署参数的配置,而更像是一个“防御能力很强,但副作用也很明显”的实验点。

\subsection{结果讨论}

除了准确率变化之外,\(\tau\) 的统计也提供了有用信息。当前实验中,对抗样本平均 \(\tau\) 为 1.0470,高于干净样本的 0.9453。这个方向与 TTC 的基本假设是一致的,说明桥接到当前动态提示模型后,\(\tau\) 仍然能够在一定程度上反映输入稳定性。换句话说,TTC 在本文模型上并不是“盲目加扰动”,而是确实利用了特征变化信号。

另一方面,干净样本的 \(\tau\) 也不低,这从侧面解释了为什么 \texttt{clean\_ttc\_acc} 会下降较多。对细粒度宠物分类任务来说,类别之间本就接近,图像在特征空间中的边界更敏感。TTC 一旦采用偏强的参数,很容易把一部分原本能正确分类的干净样本也推离当前最合适的表示区域。因此,安全部分在当前阶段更适合被理解为一种可选扩展: 它在强攻击下有效,但是否启用以及采用哪组参数,需要根据对鲁棒性和干净精度的取舍来决定。

\section{本章小结}

本章介绍了本文系统的安全扩展设计。与项目早期的项目内重实现不同,最终采用的是 TTC 原始代码桥接方案,通过 \texttt{evaluate\_security\_ttc.py} 将 OxfordPets 测试集、DynamicPromptTrainer 模型和 TTC 防御流程连接起来。实验结果表明,在 PGD 攻击下,TTC 能将模型准确率从 0.08\% 提升到 71.63\%,说明该防御方法在当前细粒度模型上具有明显效果; 同时,干净样本准确率下降到 68.06\%,也说明安全增强伴随着较大的精度代价。这个结论为后续讨论系统局限性和改进方向提供了更具体的依据。
